<% if ((is_home() || is_category() || is_tag()) && page.total > 1) { %>
  <nav class='paginator-wrap dis-select<%- scrollreveal(' ') %>' aria-label='文章分页导航'>
    <%- paginator({
      prev_text: icon('default:arrow-left'),
      next_text: icon('default:arrow-right'),
      force_prev_next: true,
      escape: false
    }) %>
  </nav>
<% } %>
